{
  "scripts": {
    "k8s:generate": "cd kubernetes && ts-node manifest.ts",
    "k8s:dev": "npm run k8s:generate && skaffold dev"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
